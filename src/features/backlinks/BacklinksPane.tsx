export interface BacklinksPaneProps {
  path: string;
}

export function BacklinksPane({ path }: BacklinksPaneProps) {
  return <div className="pane-placeholder">BacklinksPane for {path} (not implemented yet)</div>;
}
