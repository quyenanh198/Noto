export interface OutlinePaneProps {
  path: string;
}

export function OutlinePane({ path }: OutlinePaneProps) {
  return <div className="pane-placeholder">OutlinePane for {path} (not implemented yet)</div>;
}
