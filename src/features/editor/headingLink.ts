// Lives in core so the renderer can match `![[Note#heading]]` sections the same way navigation does; re-exported for the editor.
export { headingLinkText, headingsMatch } from '../../core/markdown/headingLink';
